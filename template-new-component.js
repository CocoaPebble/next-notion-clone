// Create a demo object component as the default export
"use client";

export const Demo = ({ }) => {
    return (
        // demo button
        <div>
            <button>Click Me</button>
        </div>
    )
}